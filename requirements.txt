# Runtime dependencies for Typing Adventure.
#
# PySide6 is the only hard requirement. hidapi is what lets the game talk to
# the Logitech PRO X 60's HID++ interface; without it the game still runs and
# falls back to on-screen highlighting.

PySide6>=6.6
hidapi>=0.14

# Packaging (only needed to build the .app)
# pyinstaller>=6.3
